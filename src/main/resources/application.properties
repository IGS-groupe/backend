spring.application.name=backend
server.port=4000
# DATASOURCE (DataSourceAutoConfiguration & DataSourceProperties)
spring.datasource.url=jdbc:mysql://localhost:3306/igs?useSSL=false&serverTimezone=UTC&useLegacyDatetimeCode=false
spring.datasource.username=root

# Hibernate

# The SQL dialect makes Hibernate generate better SQL for the chosen database
spring.jpa.properties.hibernate.dialect = org.hibernate.dialect.MySQLDialect

# Hibernate ddl auto (create, create-drop, validate, update)
spring.jpa.hibernate.ddl-auto = update

#security 
auth.secret=global.sec-key
auth.access.expiration=1800
auth.refresh.expiration=1800000

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=moutiasaad481@gmail.com
spring.mail.password=twokdcmdjxpgxhsc
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
# Upload path for images (make sure this directory exists or is created dynamically)
upload.path=uploads
spring.servlet.multipart.max-file-size=5MB
spring.servlet.multipart.max-request-size=5MB
spring.servlet.multipart.enabled=true
spring.jackson.serialization.fail-on-empty-beans=false
spring.jpa.open-in-view=false